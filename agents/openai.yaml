interface:
  display_name: "Semantic Diagram Builder"
  short_description: "Build semantic object-relation SVG diagrams"
  default_prompt: "Use $semantic-diagram-builder to create a semantic diagram from these objects, groups, and relationships."

policy:
  allow_implicit_invocation: true
